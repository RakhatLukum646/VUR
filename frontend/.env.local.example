# Copy to .env.local for local development (npm run dev)
# In Docker these are not needed — Nginx gateway handles routing.

VITE_LLM_URL=http://localhost:8002
VITE_AUTH_URL=http://localhost:8003
VITE_WS_URL=ws://localhost:8001
