/**
 * Brand assets live in `frontend/public/` (Vite serves them at `/…`).
 * Favicon: `public/logo.png` → link in `index.html`.
 */

/** Product name in the header (keep short on small screens). */
export const BRAND_NAME = 'SignZhan';

/**
 * Longer line under the title — safe to make a full product sentence.
 * Edit here so it stays consistent across the app header.
 */
export const BRAND_TAGLINE =
  'Real-time Russian Sign Language (RSL) to text translation';

/**
 * Wide wordmark in the menu bar: `frontend/public/SignZhan.png`.
 * Shown in a fixed-height strip so top/bottom are clipped slightly.
 */
export const BRAND_WORDMARK_SRC = '/SignZhan.png';
