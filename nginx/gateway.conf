upstream frontend {
    server frontend:8080;
}

upstream mediapipe {
    server media-pipe:8001;
}

upstream llm {
    server llm-service:8002;
}

# Redirect all HTTP → HTTPS
server {
    listen 80;
    server_name localhost;
    # docker-compose maps 443 -> 4443 on host, so include port in redirect
    return 301 https://$host:4443$request_uri;
}

server {
    listen 443 ssl;
    server_name localhost;

    ssl_certificate     /etc/nginx/certs/server.crt;
    ssl_certificate_key /etc/nginx/certs/server.key;
    ssl_protocols       TLSv1.2 TLSv1.3;
    ssl_ciphers         HIGH:!aNULL:!MD5;

    # ── Auth Service ──────────────────────────────────────────
    # Docker can change container IPs after recreate; static upstream names
    # are resolved once at nginx start → 502 until gateway restart. Resolver +
    # variable forces re-resolve on each request (Docker embedded DNS).
    location /auth/ {
        resolver 127.0.0.11 valid=10s ipv6=off;
        set $auth_upstream auth-service:8003;
        proxy_pass http://$auth_upstream;
        proxy_set_header Host              $host;
        proxy_set_header X-Real-IP         $remote_addr;
        proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # ── LLM (translation) REST API ────────────────────────────
    location /api/v1/ {
        proxy_pass http://llm/api/v1/;
        proxy_set_header Host              $host;
        proxy_set_header X-Real-IP         $remote_addr;
        proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # ── MediaPipe WebSocket endpoint ─────────────────────────
    location /ws/ {
        proxy_pass http://mediapipe/ws/;
        proxy_http_version 1.1;
        proxy_set_header Upgrade    $http_upgrade;
        proxy_set_header Connection "Upgrade";
        proxy_set_header Host       $host;
        proxy_read_timeout 3600s;
        proxy_send_timeout 3600s;
    }

    # ── Health checks ────────────────────────────────────────
    location /health {
        proxy_pass http://llm/health;
        proxy_set_header Host $host;
    }

    location /health/mediapipe {
        proxy_pass http://mediapipe/api/v1/health;
        proxy_set_header Host $host;
    }

    # ── Frontend SPA (catch-all) ─────────────────────────────
    location / {
        proxy_pass http://frontend/;
        proxy_set_header Host              $host;
        proxy_set_header X-Real-IP         $remote_addr;
        proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
